<% content_for :title, "Comments" %>

<div class="max-w-2xl mx-auto" data-controller="confirm-delete">
  <div class="flex items-center justify-between mb-4">
    <h1 class="page-title">Comments</h1>
  </div>

  <%= form_with url: comments_path, method: :get, class: "search-form mb-4",
                data: { turbo_frame: "comments-results" } do |f| %>
    <div class="search-input-wrapper">
      <%= image_tag "search.svg", class: "search-icon", alt: "" %>
      <%= f.search_field :q, value: @query,
            placeholder: "Search comments…",
            class: "search-input",
            autofocus: true,
            onsearch: "this.form.requestSubmit()" %>
    </div>
  <% end %>

  <%= turbo_frame_tag "comments-results" do %>
    <% if @query.present? %>
      <p class="text-hint mb-3">
        <%= @comments.size %> result<%= @comments.size == 1 ? "" : "s" %> for "<%= @query %>"
      </p>
    <% end %>

    <div class="space-y-4">
      <% unless @query.present? %>
        <div class="card p-5">
          <%= render "form", comment: @comment, url: comments_path %>
        </div>
      <% end %>

      <% if @comments.empty? %>
        <div class="card p-8 text-center text-gray-400">
          <% if @query.present? %>
            No comments found for "<%= @query %>".
          <% else %>
            No comments yet. Be the first to comment!
          <% end %>
        </div>
      <% else %>
        <%= render @comments %>
      <% end %>
    </div>
  <% end %>

  <%= render "shared/confirm_delete_modal" %>
</div>
